start: struct+

struct: "struct" NAME "{" field+ "}"

field: TYPE NAME ";"

TYPE: "float" | "int" | "double" | "string" | "vec3" | "vec4" | "mat3" | "mat4" | "quat"

%import common.CNAME -> NAME
%import common.WS
%ignore WS